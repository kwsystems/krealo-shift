{
  "name": "krealo-shift-functions",
  "private": true,
  "main": "lib/functions/src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.6.0",
    "krealo-shift": "file:.."
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~5.9.0"
  }
}
